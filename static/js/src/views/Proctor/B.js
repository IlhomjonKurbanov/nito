var React = require('react');
var Router = require('react-router');
var { Link } = Router;

module.exports = React.createClass({
  render: function () {
    return (
      <div>
        Station B stuff
      </div>);
  },

  styles: {
  }
});
